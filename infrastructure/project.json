{
  "name": "infrastructure",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "infrastructure",
  "tags": ["infrastructure"],
  "targets": {
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node infrastructure/scripts/migrate.ts"
      }
    }
  }
}
